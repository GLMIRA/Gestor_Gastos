#ifndef CONSTANTES_HPP
#define CONSTANTES_HPP

#include <ctime>

constexpr int NUM_MAX_USER = 10;
constexpr int NUM_MAX_EXPENSE = 20;
constexpr int NUM_MAX_SOURCESOFINCOME = 5;
inline const time_t MINIMUM_AGE_SECONDS = 18LL * 365 * 24 * 60 * 60; // 18 anos

#endif
